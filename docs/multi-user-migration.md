# 多用户改造实时记录

> 本文件是「用户空间数据隔离 + 角色迁移」阶段的实时改造记录:每完成一步立即更新状态,
> 作为架构决策与验收依据。上一阶段(账号体系)已结束,见阶段总览。

## 阶段总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 账号体系(邮箱+密码+Argon2+httpOnly Cookie+Turnstile) | ✅ 已完成(2026-08-22) |
| 2 | 用户空间数据隔离 + 角色迁移 | 🔄 进行中 |
| 3 | 前端工作区(我的星云 / 公共星云切换) | ⬜ 并入阶段 2 |
| 4 | 聚合层(实体对齐 / 公共星云合并) | ⬜ 后续阶段 |

## 阶段 2:用户空间数据隔离与角色迁移

### 设计原则

- **主数据模式**:业务表(authors / works / edges)增加 `owner_id`(NULL = 公共/策展数据);
  用户空间是私有叶子,公共星云是聚合视图,删除私有数据不影响公共数据。
- **隔离在查询层强制**:所有空间数据接口必须带 owner 上下文,漏配即返回空而非泄露;
  公共接口(/api/graph 等)保持不变、无需登录。
- **角色**:`users.role` 已有 `user` / `admin`;管理接口从单一 `ADMIN_TOKEN` 迁移为
  admin 角色校验,审计 `actor` 记录真实用户。

### 步骤清单(实时状态)

1. Schema v9:业务表加 `owner_id` + 索引,贡献表加归属 ✅
2. 数据层:行级 CRUD 支持 owner 上下文、级联删除/恢复按 owner 约束、
   读缓存按「DB 路径 + 空间」分键、任何空间写入都失效缓存 ✅
3. 后端 API:个人空间 `/api/me/*`(图/搜索/详情/扩散/路径 + 行级 CRUD),
   公开接口保持全局;公共星云 = admin 空间(未认领历史行也视为公共) ✅
4. 贡献归属:登录用户提交自动带 `user_id`,未登录匿名;列表带 `user_email` ✅
5. 角色迁移:移除 ADMIN_TOKEN/HTTPBearer;`require_admin` 角色依赖;
   `ADMIN_BOOTSTRAP_EMAIL` 注册自动提权 + 启动时补角色 + 认领未归属数据 ✅
6. 前端:登录后「我的星云」入口 + 空间切换(公共/我的,API 按空间前缀)、
   数据管理按 admin 角色显隐、移除令牌授权框 ✅
7. 测试与文档:隔离测试(越权 404/403、跨空间引用拒绝、公共/私有互不可见)✅
   README / .env.example / deploy / data_schema / to-do 更新 ✅

### 已确认决策(2026-08-22)

- **Q1 个人空间初始状态**:空空间——用户从零搭建自己的星云。
- **Q2 可见性**:默认仅自己可见(私有);公开/发布机制留到聚合层阶段。
- **Q3 首个管理员**:`.env` 配置 `ADMIN_BOOTSTRAP_EMAIL`,该邮箱注册自动提权为
  `admin` 角色;启动时若该邮箱已存在则自动补齐角色。
- **Q4 ADMIN_TOKEN**:彻底移除——管理接口只接受 admin 角色登录态;前端管理入口
  由登录用户角色驱动,删除令牌授权框与 adminAuth 模块。
- **现有数据归属**:当前全部策展数据(authors/works/edges)认领为
  `ADMIN_BOOTSTRAP_EMAIL` 用户的个人空间;**公共星云 = admin 的空间**,
  未登录游客仍可浏览(owner_id 为空的历史行在认领前也视为公共)。
- **贡献归属**:登录用户提交「点亮星空」自动带 `user_id`;未登录仍可匿名提交。
- **前端空间切换**:左侧栏「公共星云 / 我的星云」切换;我的星云需登录。
- **数据表结构(2026-08-22 补充)**:保持单表 + owner_id,不拆两套表;
  新增 `users.space_visibility`(默认 `public`),星际跃迁只读公开星云。
- **用户数据 → 公共星云(2026-08-22 补充)**:后台流程(AI 预审 + 人工确认),
  用户无需手动提交;公共星云不收录用户隐私数据——书籍关联/信息是客观事实,
  收录时只取客观字段,必要时内部保留溯源(source_user_id/source_row_id),
  对外 API 不暴露。

### 变更日志

- 2026-08-22:阶段 1(账号体系)验证完成——后端 101 测试 + 前端 57 测试 + lint/typecheck/build
  全绿;真实服务端到端注册/登录/登出通过;Turnstile 密钥已配置并验证强制校验生效。
- 2026-08-22:创建本记录,开始阶段 2;提出 4 个待确认设计问题(Q1-Q4)。
- 2026-08-22:Q1-Q4 已确认(见上),补充两条默认决策(贡献归属、空间切换入口)。
  按「空空间 + 私有 + 引导邮箱提权 + 去 token + 现有数据归 admin」模型开工。
- 2026-08-22:后端完成——107 测试全绿 + ruff 通过。隔离模型:admin 空间即公共星云;
  用户空间私有;写路径统一走 `app/space_crud.py`;修复用户空间写入未失效读缓存的问题。
- 2026-08-22:前端完成——空间切换、角色驱动管理入口、删除 adminAuth 模块;
  typecheck/lint/54 测试/build 全绿。
- 2026-08-22:端到端验证通过——存量数据已自动认领到引导管理员(unowned=0);
  用户 A 私有空间 2 节点、用户 B 空间为空、越权改 404、管理接口 403、公共零泄露。
- 2026-08-22:修复发现的问题——`PUBLIC_REVIEWED_ONLY` 过滤误作用于个人空间,
  已改为仅约束公共视图(用户必须能看到自己空间的草稿/驳回数据)。
- 2026-08-22:阶段 2 完成(步骤 1-7 全部 ✅);服务以正式配置运行中。
- 2026-08-22:按用户需求,数据管理视图开放给所有登录用户——非 admin 通过
  `/api/me/*`(新增 `/api/me/data`)管理自己的星云;贡献审核/日志/快照保持
  admin 专属。108 后端测试 + 54 前端测试 + lint/build 全绿。
- 2026-08-22:点亮星空入口改为个人数据添加——提交直接写入 `/api/me/*`;
  下拉框无匹配时第一行「添加新作品 / 新作者」打开标准新增弹窗;
  从 Admin 抽取 NodeFormModal 与数据管理共用(Admin chunk 31.8kB → 18.3kB)。
- 2026-08-22:修复下拉列表丢失问题——数据源改为「公共星云 + 自己的数据」合并:
  列表恢复可搜索/筛选;选中公共作品/作者时自动复制一份到本人空间
  (后端隔离要求引用必须归属自己);无匹配时仍显示「添加新节点」第一行。
- 2026-08-22:确认单表方案;实施可见性标志(默认 public)与星际跃迁只读接口
  (`/api/space/{user_id}/graph`、`/api/space/random/graph`)。
- 2026-08-22:全量遗留审查与修复——
  - 修复:CSV 导出只含公共星云(此前若 admin 写入会带出用户私有行,已加过滤);
  - 修复:`deploy.sh` 不再从 CSV 重建 SQLite(避免每次部署清空用户星云),迁移由启动自动执行;
  - 修复:CSV 快照恢复改为只重建公共星云、保留用户私有行(新增 `replace_public_rows`);
  - 文档同步(README / DEPLOY.md / data_schema / .env.example)。
  - 遗留说明:`.env` 中旧 `ADMIN_TOKEN` 无害可删;admin「贡献」Tab 与
    `/api/contribute/echo` 保留为兼容入口(前端已不使用,待后台发布管线取代)。
- 2026-08-22:星际跃迁功能落地——左侧栏「公共星云 / 我的星云」下方新增
  「✦ 星际跃迁」按钮;数据源标签改为所在星云账号(公共星云显示 public);
  空间接口附 `displayName`(当前用邮箱,公网部署前建议脱敏/显示名)。
  生成测试账号 jump-test-1/2/3@test.local(密码 jump-test-123,各 3-5 位作者
  + 若干作品/涟漪),随机跃迁 10 次命中 5 个公开星云,定向访问与 404 均正常。
